import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class HomeAndFlatPage {
    WebDriver driver;

    public HomeAndFlatPage(WebDriver driver) {
        this.driver = driver;
    }
}
